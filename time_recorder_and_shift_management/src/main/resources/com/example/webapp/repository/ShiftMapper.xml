<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.webapp.repository.ShiftMapper">

	<select id="selectAllShiftsByYearMonth">
		SELECT *
		FROM shift_and_timestamp${year}
		WHERE
		date='${year}-${month}';
	</select>
	<select id="selectAllShiftsByYearMonthAndEmployeeId">
		SELECT *
		FROM shift_and_timestamp${year}
		WHERE
		date='${year}-${month}'
		and
		employee_id=${employee_id};
	</select>

</mapper>